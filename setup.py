from setuptools import setup, find_packages

setup(name='get-mid-point',
      version='0.0.1',
      description='',
      author='',
      author_email='',
      url='',
      packages=find_packages()
      )
